// App #1 (Cloudflare Workers) is scaffolded in Prompts 6–7 of the handoff.
// Intentionally near-empty for now; the workspace member exists so the
// monorepo boundary is set on day one and core is never trapped inside Workers code.
export {};
