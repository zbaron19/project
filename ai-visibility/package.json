{
  "name": "ai-visibility",
  "version": "0.0.0",
  "private": true,
  "description": "AI-Visibility engine + apps monorepo. packages/core is runtime-agnostic.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@10.33.0"
}
